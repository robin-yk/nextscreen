# ── Install the nextscreen package itself ─────────────────────────────────────
-e .

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.30

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scipy>=1.10
openpyxl>=3.1

# ── ML / feature selection ────────────────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44

# ── Plotting ──────────────────────────────────────────────────────────────────
plotly>=5.0
matplotlib>=3.7

# ── Bayesian Optimisation (CPU-only torch to stay within cloud memory limits) ─
# gpytorch and botorch are intentionally unpinned — let nextorch resolve
# compatible versions (nextorch==0.4.0 requires botorch<=0.4.0).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
nextorch==0.4.0

# ── Report generation ─────────────────────────────────────────────────────────
# weasyprint>=60.0  # requires system libs (cairo, pango) — not available on cloud
